version: "3.9"
services:
  openshift-ci-job-re-trigger:
    image: quay.io/redhat_msi/openshift-ci-job-re-trigger
    container_name: openshift-ci-job-re-trigger
    restart: unless-stopped
    ports:
      - "5000:5000"
